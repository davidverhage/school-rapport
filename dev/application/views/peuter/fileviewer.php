<div id="fullpage" class="container-fluid">

    <div class="row section" id="section0">
        <div class="col-xs-12 col-sm-12">
            <h1>Bestanden Selecteren</h1>
            <p>
                If CSS3 transforms are supported (e.g. on mobile devices) they will be used for animations, usually a
                good
                choice if the animation on mobile devices is slow.
            </p>
        </div>
    </div>

    <div class="row section" id="section1">
        <div class="col-xs-12 col-sm-6">
            <h1>Geselecteerde bestanden</h1>
            <p>
                If CSS3 transforms are supported (e.g. on mobile devices) they will be used for animations, usually a
                good
                choice if the animation on mobile devices is slow.
            </p>
        </div>
        <div class="col-xs-12 col-sm-6">
            <h1>Actie</h1>
            <p>
                If CSS3 transforms are supported (e.g. on mobile devices) they will be used for animations, usually a
                good
                choice if the animation on mobile devices is slow.
            </p>
        </div>
    </div>

    <div class="row section" id="section2">
        <div class="col-xs-12 col-sm-6">
            <h1>Klaar</h1>
            <p>
                If CSS3 transforms are supported (e.g. on mobile devices) they will be used for animations, usually a
                good
                choice if the animation on mobile devices is slow.
            </p>
        </div>
    </div>
</div>
